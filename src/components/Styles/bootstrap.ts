import { Button } from "react-bootstrap";
import styled from "styled-components";

export const ButtonBt = styled(Button) `
  background-color: #8A2BE2	;
  color: white; // apliquei essa cor somente para teste, pois queria saber se dava pra usar styled-components com bootstrap o.O e da.
                // é só passar o Button fornecido pelo bootstrap como parametro no styled que podemos estiliza-lo
`